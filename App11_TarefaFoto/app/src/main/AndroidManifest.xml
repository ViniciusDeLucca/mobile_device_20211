<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.viniciusalmeida.app11_tarefafoto">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_floppy_disk"
            android:label="@string/app_name"
            android:roundIcon="@drawable/ic_floppy_disk"
            android:supportsRtl="true"
            android:theme="@style/Theme.App11_TarefaFoto" tools:ignore="AllowBackup">
        <activity android:name=".CadastroAct"
                  android:label="@string/titulo_cadastro">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>